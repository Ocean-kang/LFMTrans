seed: 2025

deice_gpu: 0

knngraph:
  k: 10
  sigma: 1
  knn_fn: ip # 'ip' or 'L2'
  W_full_fn: ip # 'ip' or 'L2'
  W_fn: raw # 'heat kernal' or 'inv' or 'raw'

laplacian_mat:
  k: 20 # K eigenvectors for using in decomposition
