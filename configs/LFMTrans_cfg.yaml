seed: 2025

deice_gpu: 0

knngraph:
  k: 10
  sigma: 1
  knn_fn: ip # 'ip' or 'L2'
  W_full_fn: ip # 'ip' or 'L2'
  W_fn: heat_kernel # 'heat_kernal' or 'inv' or 'raw'

laplacian_mat:
  k: 150 # K eigenvectors for using in decomposition

fm_retrieval:
  metric: ip # 'ip' or 'L2'